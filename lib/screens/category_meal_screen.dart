import 'package:flutter/material.dart';

class CategoryMealScreen extends StatelessWidget {

  @override 
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(title: Text('Reciepe'),),
      body: Center(
       child: Text('THe Category Reciepe page')
      ) ,
    );
  }
}